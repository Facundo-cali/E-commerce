<!DOCTYPE html>
<html lang="en">
<!-- Include Head -->
<%- include('partials/head', {title: 'Login'}) %>
<script src="/javascripts/login.js" defer></script>
<body>
    <!-- Header -->
	<%- include('partials/header') %>
	<!-- /Header -->

    <main class="main__login">
        <form class="form__login" action="/users/login" method="post">
            <% if(typeof errors != "undefined"){ %>
                <% errors.forEach(error => { %>
                    <p><%= error.msg %> </p>
                <% }) %>
            <% } %> 
            <h1 class="form__h1">BIENVENIDO</h1>
            <div class="form__completar">
                <label>Email
                    <input class="form__completar" type="email" name="email" id="email" placeholder="Email">
                    <small></small>
                </label>
            </div>
            <div class="form__completar">
                <label>Contraseña
                    <input class="form__completar" type="password" name="password" id="password" placeholder="Contraseña">
                    <small></small>
                </label>
            </div>
            <div class="recordarme">
                <label for="">Recordame</label>
                <input type="checkbox" name="recordarme">
            </div>
            <button class="form__button" type="submit" value="login">Ingresar</button>
        </form>
    </main>

</body>